# Session handoff — B119 corrective `0.0.77` and native X4 rerun close

Date: `2026-09-08`
Project: `F:\DEV_ENV\X4_Forge`
Status: the corrective `0.0.77` release/native sheet unit is `VERIFIED`; the supplied literal brief is `6/6
VERIFIED`; overall B119 remains `IN_PROGRESS / PARTIAL`.

## Session-start brief

- **Project identity:** X4 Forge B119, the linter-first source-faithful X4 Lua UI editor; GitHub owner #41.
- **Eyeball queue:**
  1. Full twelve-reference AI Influence reconstruction/current-game visual census remains open. Click-by-click
     Ken check: open the B119 plan and retained native evidence, inspect each `00` and `1a`-`1j` reference beside the
     corresponding current X4/source result, classify every image, and record any source-backed correction before
     calling parity. Do not promote Forge preview or one native sheet to universal truth.
  2. Universal C++ acceptance and arbitrary Lua/Helper/widget coverage remain open. Click-by-click Ken check for any
     proposed player-facing claim: inspect the exact deployed source/path/profile in X4, then compare the scoped log
     and screenshot; mark only the observed surface as closed.
- **Commit question:** baseline `HEAD == 573a577a56f21cbe618cd42061cbf7d5d96423e6`. This documentation close is
  uncommitted. Parent must review the six owned-file diff, perform any authorized external projections, then exact-
  stage and commit/push; do not invent the future commit hash.

## Verified corrective state

- Stable `0.0.77` was built, published exactly once on OpenVSX, independently downloaded with exact local/public
  parity, installed in Antigravity, and passed installed runtime oracles `134/134`.
- Stable VSIX: `F:\DEV_ENV\X4_Forge\vscode-extension\x4-forge-studio-0.0.77.vsix`, `26,321,722` bytes,
  SHA-256 `D1349AC2A3D43FEFD07CAA64BF262FFF4D4F10DEE6E38F453DC0EDAC737486F6`.
- Full isolated serial E2E passed `106/106` on `3200/3201` with complete `verdict/treeGone` and unchanged live
  state. Precommit passed before this docs close.
- Repaired `x4_ai_influence` deployment passed all `11` checks. An independent whole-tree census found `124`
  identical common files and only the intentional `.forgekeep` difference. Source and deployed `aic_sheet.lua` are
  both `12,626` bytes, SHA-256 `A09A66B4BF98491B627304FD0F198B3893A21F9EE18BF8AA0979BB82220D4E34`.
- The temporary `pipeline_test` launcher deployment also passed all `11` checks and an exact source/deployed
  four-file census before recovery was consumed.
- X4 9.00 visibly rendered the repaired full agreement sheet through the native Helper/widget/C++ route at drawable
  `2544x1353` (Steam capture `2544x1354`), UI scale `1.0`. Visible: `TERMS OFFERED`, `B119-FIXTURE-0001`, four
  clauses/notes, `WHAT THIS COSTS YOU`, four delta rows, and three native buttons.
- Native evidence directory: `F:\DEV_ENV\X4_Forge\dev-docs\b119-ai-influence-dogfood\in-game-20260908-scrollbar-clean\`.
  Screenshot `x4-native-agreement-sheet-2544x1354.jpg`: `553,321` bytes, SHA-256
  `989C03A39F162F8A42121243797896084EBFBB64650C3A08B8CD9E01FE961C7B`. Log `debuglog-current-session.txt`:
  `16,859` bytes, SHA-256 `5C00FFB818A23BB19B84D67D4373F06EE199329343BB01755217BABF3B14AB2F`.
- The current-session log has exactly one each of launcher armed, dependency-ready, fixture-ready `clauses=4 diff=4`,
  open-requested, `[AICHAT][SHEET] display DONE clauses=4`, and launcher opened. It has zero `reserveScrollBar`,
  `DisplayView`, `Failed to set up the view`, `stack traceback`, `Lua Error`, launcher refusal, or timeout diagnostics.
- Temporary recovery was consumed; game `pipeline_test` is absent and X4 process count is `0`. The scratch fixture
  was restored exactly through Forge strict CAS: `content.xml` `367`/`23A7E9...A5034`, README `210`/`31B80A...C871`,
  `ui.xml` `273`/`655331...1689`, and Lua `5,488`/`C1D9CD...2718E`.

## Durable records and boundaries

- The canonical plan records the close in `docs/plans/2026-09-05-b119-ai-influence-visual-dogfood.md`; BACKLOG's
  current B119 checkpoint now says `0.0.77`/native unit `VERIFIED` and full B119 `PARTIAL`.
- `F:\StarForge\wiki\x4-forge\capability-map.md` records a bounded byte-authoritative real-mod/native-sheet delta.
  `F:\StarForge\wiki\x4-forge\aar-log.md` records sustain, work/approach, tools, highest-risk, and all close
  triggers. The UI gotchas guide updates cards 9 and 15 in place and adds only cards 61-62.
- GitHub #41 remains open. External GitHub/Notion/Drive projections and the final source commit are parent actions;
  no new remote IDs, revisions, or commit hashes are recorded here.
- Overall B119 is not complete: the twelve-reference reconstruction/current-game census, universal C++ acceptance,
  and arbitrary Lua/Helper/widget coverage remain open. The exact native sheet proof is one source/path/profile,
  not a universal 1:1 or all-Lua claim.

## Exact next unit

Parent close actions are to reconcile this six-file diff, perform any authorized external projections, and commit/push
with exact path selection. The next substantive B119 unit is the full twelve-reference AI Influence reconstruction and
current-game visual census, with source-backed corrections only; it must retain the current native/package boundaries.

## Hazards

- Forge strict CAS compares the supplied digest string lexically with its lowercase digest: uppercase-equivalent
  `expectedSha256` falsely returned `409 FILE_CHANGED` without mutation; use the canonical lowercase digest.
- Direct X4 `Start-Process` produced no log because Steam showed its custom-launch-arguments confirmation. Require the
  native modal to be continued, then verify process/window/current-session log before claiming launch or render.
- OpenVSX propagation lag is not a reason to republish an accepted version. The independent public download is the
  parity authority. The locked SQLite file was excluded from the independent whole-tree hash by design.
- The E2E harness uses `3200/3201` when the default ports are occupied; never touch unrelated listeners or run a
  second stack in parallel. Do not launch apps or rerun game/deploy work from this docs handoff.
- Preserve the unrelated showcase artifacts and every other pre-existing modified, deleted, or untracked path.
  This worker changed only the six authorized documentation paths; do not revert, stage, commit, push, publish, or
  rewrite Git metadata.

## Preservation boundary

The baseline dirty tree includes unrelated onboarding/bug/data/docs/scripts changes, server and test changes,
`test-results/.last-run.json`, extension evidence, `artifacts/`, `media/`, package-workspace files, lockfiles, and
untracked showcase/marketing files. They are user-owned and remain untouched. No mod/game directory, retained
screenshot/log, global workflow AAR, GitHub, Notion, Drive, or remote service was changed by this worker.
