{
  "name": "x4-forge-studio",
  "displayName": "X4 Forge Studio",
  "description": "X4: Foundations modding studio for visual Mission Director graphs, native X4-aware XML editing, deterministic validation, safe patches, and guarded package and deploy workflows.",
  "version": "0.0.75",
  "icon": "icon.png",
  "publisher": "x4forge",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/KennyG1990/X4_Forge.git"
  },
  "bugs": {
    "url": "https://github.com/KennyG1990/X4_Forge/issues"
  },
  "keywords": [
    "x4",
    "x4 foundations",
    "x4 forge",
    "x4 modding",
    "egosoft",
    "mission director",
    "xml",
    "visual modding",
    "mod validation",
    "nexus mods",
    "steam workshop"
  ],
  "galleryBanner": {
    "color": "#0d1117",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onWebviewPanel:x4forge.studio"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "x4forge",
          "title": "X4 Forge Studio",
          "icon": "media/activitybar.svg"
        }
      ]
    },
    "views": {
      "x4forge": [
        {
          "id": "x4forge.launcher",
          "name": "Open Studio",
          "icon": "media/activitybar.svg"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "x4forge.launcher",
        "contents": "Author, validate, package, and deploy X4: Foundations mods in one local workspace.\n\n[Open Studio](command:x4forge.openStudio)\n\nThe Studio connects a visual Mission Director graph with native X4-aware XML editing, deterministic diagnostics, and guarded package and deploy workflows. Configure your X4 installation and reference/schema data in Studio Settings.\n\n[Open Mod Folder](command:x4forge.openModFolder)\n[Create Agent Key](command:x4forge.createAgentKey)\n[Show Backend Logs](command:x4forge.showLogs)\n[Stop Local Backend](command:x4forge.stopSidecar)"
      }
    ],
    "commands": [
      {
        "command": "x4forge.openStudio",
        "title": "X4 Forge: Open Studio"
      },
      {
        "command": "x4forge.stopSidecar",
        "title": "X4 Forge: Stop Local Backend"
      },
      {
        "command": "x4forge.showLogs",
        "title": "X4 Forge: Show Backend Logs"
      },
      {
        "command": "x4forge.showCapabilities",
        "title": "X4 Forge: Show IDE Capabilities"
      },
      {
        "command": "x4forge.createAgentKey",
        "title": "X4 Forge: Create Agent Key"
      },
      {
        "command": "x4forge.openModFolder",
        "title": "X4 Forge: Open Mod Folder in Workspace"
      },
      {
        "command": "x4forge.copyMcpConfig",
        "title": "X4 Forge: Copy MCP Config for Agents"
      },
      {
        "command": "x4forge.refreshAgentBrief",
        "title": "X4 Forge: Refresh Agent Brief"
      },
      {
        "command": "x4forge.generateProof",
        "title": "X4 Forge: Generate Proof Report"
      },
      {
        "command": "x4forge.checkModConflicts",
        "title": "X4 Forge: Check Installed Mod Conflicts"
      }
    ],
    "configuration": {
      "title": "X4 Forge Studio",
      "properties": {
        "x4forge.attachUrl": {
          "type": "string",
          "default": "http://127.0.0.1:3000",
          "description": "Attach to an already-running local Forge before starting a sidecar. The extension attaches only after this URL answers the Forge agent-schema probe and never stops a server it attached to."
        },
        "x4forge.forgeRoot": {
          "type": "string",
          "default": "",
          "description": "Optional path to a built Forge checkout containing dist/server.cjs and node_modules. Leave empty to use the app bundled inside the extension."
        },
        "x4forge.stateDir": {
          "type": "string",
          "default": "",
          "description": "Directory for the sidecar's workspace state (X4_STATE_DIR). Leave empty for per-user extension storage. Point it at an existing Forge checkout's .studio-state only while that checkout's server is stopped; the attach probe normally prevents unsafe sharing."
        },
        "x4forge.nodePath": {
          "type": "string",
          "default": "",
          "description": "Path to the Node.js executable used to run the sidecar. Leave empty to use node from PATH. The sidecar requires a real Node installation with ABI-compatible native modules."
        },
        "x4forge.autoOpen": {
          "type": "boolean",
          "default": false,
          "description": "Open the X4 Forge Studio automatically when a (trusted) workspace finishes loading. Useful for dedicated modding workspaces; off by default."
        },
        "x4forge.debug": {
          "type": "string",
          "enum": [
            "off",
            "inspect",
            "inspect-brk"
          ],
          "default": "off",
          "enumDescriptions": [
            "Normal mode; no debugger.",
            "Launch the backend with --inspect and auto-attach the Node debugger (breakpoints, stepping). Works in VS Code and Antigravity.",
            "Like inspect, but pause at the first line so you can debug startup; continue execution in the debugger to start the server."
          ],
          "description": "Attach a Node debugger to the managed backend sidecar. For SOURCE-LEVEL (TypeScript) breakpoints, also set x4forge.forgeRoot to a built repo checkout (its bundle keeps sourcemaps); against the bundled app you debug at the bundled-JS level. Default off."
        },
        "x4forge.writeXmlAssociations": {
          "type": "boolean",
          "default": false,
          "description": "When opening a mod folder, optionally write per-file xml.fileAssociations for applicable game XSDs (plain-rooted factions, gamestarts, and addon files; never diff patches). Requires an XML extension to use these associations. Off by default; Forge validation remains authoritative."
        },
        "x4forge.twoWayEditing": {
          "type": "boolean",
          "default": true,
          "description": "When authored text changes on disk, offer an explicit guarded import into the canvas. Compare-and-swap protection refuses lossy imports or concurrent canvas changes instead of overwriting."
        }
      }
    }
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "X4 Forge compiles, validates, and packages mod projects (filesystem writes and local process execution). The extension is disabled in untrusted workspaces."
    },
    "virtualWorkspaces": {
      "supported": false,
      "description": "The Forge backend requires a local filesystem."
    }
  },
  "scripts": {
    "build": "node scripts/build-ext.mjs",
    "stage-app": "node scripts/stage-app.mjs",
    "probe-app": "node scripts/probe-staged-app.mjs",
    "package": "vsce package --allow-missing-repository",
    "inspect-package": "node scripts/inspect-vsix.mjs",
    "changelog": "node scripts/gen-changelog.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "3.9.2",
    "esbuild": "^0.25.0",
    "typescript": "~5.8.2"
  }
}
